import React, { Component } from 'react';

/**
* Hero component. Represents a generic hero
*/
class Hero extends Component {
  /**
    * Hero Component render method
    * @returns {ReactElement} markup
  */
  render() {
    return (
      <div className="hero">
        HERO
      </div>);
  }
}

export default Hero;
